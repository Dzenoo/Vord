export enum ActivityStatus {
  ONLINE = 'online',
  OFFLINE = 'offline',
  AWAY = 'away',
  BUSY = 'busy',
}

export enum TypeOfChannel {
  TEXT = 'text',
  VOICE = 'voice',
}

export enum MessageContextType {
  CHANNEL = 'channel',
  DMCHANNEL = 'dmchannel',
}

export enum TypeOfAttachment {
  IMAGE = 'image',
  VIDEO = 'video',
  FILE = 'file',
}
